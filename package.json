{
  "name": "coreonx",
  "version": "1.0.0",
  "description": "coreonx // tech_space - Landing page",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "jsdom": "^26.1.0",
    "@vitest/coverage-v8": "^3.2.1"
  }
}
